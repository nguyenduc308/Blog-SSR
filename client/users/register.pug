extends ../layout/header
block content
   h1 Sign up
   if errors
      each error in errors
         .alert.alert-danger=error
   form(action="/users/reg" method="POST")
      .form-group
         label(for="name") Name: 
         input.form-control(type="text" name="name" id="name" value=value?value.name:"")
      .form-group
         label(for="email") Email:
         input.form-control(type="text" name="email" id="email" value=value?value.email:"")
      .form-group
         label(for="password") Password:
         input.form-control(type="text" name="password" id="password" value=value?value.password:"")
      button.btn.btn-primary.mt-20 Sign up
